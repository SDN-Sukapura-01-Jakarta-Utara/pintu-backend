global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'pintu-backend'

scrape_configs:
  - job_name: 'pintu-backend'
    static_configs:
      - targets: ['host.docker.internal:3000']
        labels:
          instance: 'app'
